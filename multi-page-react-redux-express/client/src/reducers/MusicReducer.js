import {
  REQUEST_MUSIC,
  RECEIVE_MUSIC,
  FILTER_MUSIC,
  SORT_MUSIC
} from '../constants/Page'

const initialState = {
  music: [],
  displayedMusic: []
}

export default function music(state = initialState, action) {
 	//TODO
	return state
}